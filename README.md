# ICG_2025_HW4
### group2 project
## How to run
0. git clone
1. 按左下角run
2. 播放方式：按下空白鍵開始播放(需要注意輸入法)
3. 若無法手動撥放，請更改第97行`startAnimation`的狀態(預設false)
    - `startAnimation == true` 會自動播放(但可能會跳過第一幕動畫)
    - `startAnimation == false` 按下空白鍵開始播放(需要注意輸入法)